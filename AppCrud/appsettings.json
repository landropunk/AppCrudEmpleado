{
  // Configuración de registro.
  "Logging": {
    // Niveles de registro.
    "LogLevel": {
      // Nivel de registro por defecto.
      "Default": "Information",
      // Nivel de registro para los mensajes de Microsoft.AspNetCore.
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Lista de hosts permitidos.
  "AllowedHosts": "*",
  // Cadenas de conexión.
  "ConnectionStrings": {
    // Cadena de conexión a la base de datos SQL Server.
    "CadenaSQL": "Server=LENOVO-LANDROPU\\LANDROPUNKSQLSER;Database=DBCrud;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=true;"
  }
}
